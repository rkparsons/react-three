import { Card as CardBase, CardContent as CardContentBase } from '@material-ui/core'

import styled from 'styled-components'

export const Card = styled(CardBase)`
    height: 250px;
`

export const CardContent = styled(CardContentBase)`
    height: 100%;
`
